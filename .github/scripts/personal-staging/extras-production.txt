# Personal-PRODUCTION extras manifest: one pin per line, same format as
# extras.txt (staging's manifest — the two are deliberately separate files
# so a staging pin never reaches production by accident).
#
# A pin here is force-included in the production composition, overriding
# the draft gate: a listed open draft PR is composed from its live
# refs/pull/N/head. Use sparingly and remove the entry once the PR is
# marked ready for review (the open-PR stream then covers it) or lands
# upstream. Bot-authored pins are never covered by the open-PR stream;
# remove them once merged upstream, or replace them when superseded.
6856  # draft fix/background-battery-drain-main (btli) supersedes bot #5825 on v0.13.0; force-promoted into production; remove once marked ready or merged upstream
6370  # draft fix/kimi-provider-resolution (btli) force-promoted into production; remove once marked ready or merged upstream
6436  # draft fix/kimi-permission-label-main (btli) force-promoted into production; remove once marked ready or merged upstream
6439  # draft fix/kimi-steer-ctrl-s-main (btli) force-promoted into production; remove once marked ready or merged upstream
6449  # draft fix/kimi-trust-preseed (btli) force-promoted into production; remove once marked ready or merged upstream
6656  # draft feat/3d-preview-large-models (btli) force-promoted into production; remove once marked ready or merged upstream
